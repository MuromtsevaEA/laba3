#include "MinMax.h"

#include <cassert>

void
test_odin() {
    double min = 0;
    double max = 0;
    find_minmax({7}, min, max);
    assert(min == 7);
    assert(max == 7);
}

int
main() {
    test_odin();
}
